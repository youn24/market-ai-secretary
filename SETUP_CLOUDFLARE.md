# 業種別ランキングを「ザラ場中リアルタイム」にする設定（5分・無料）

業種別ランキング表を、取引時間中ずっと自動で動かすための設定です。
**Cloudflare Worker**（無料）を1つ作るだけ。一度やれば、あとはずっと自動です。

---

## なぜ必要？

ブラウザから直接Yahooのデータを取ると「CORSエラー」で弾かれます。
そこで、間に小さな中継役（Cloudflare Worker）を1つ置きます。
これにより、レポートのページが**開いている間ずっと、約30秒ごとに最新の業種別ランキング**を取りに行けるようになります。

無料枠は1日10万リクエストまで。このレポートではまったく足りるので、料金はかかりません。

---

## 手順（スマホでもPCでもOK）

### ① Cloudflareの無料アカウントを作る
1. https://dash.cloudflare.com/sign-up を開く
2. メールアドレスとパスワードを入れて登録（クレジットカード不要）

### ② Workerを作る
1. 左メニューの **「Workers & Pages」** をクリック
2. **「Create application」→「Create Worker」** をクリック
3. 名前は何でもOK（例: `market-proxy`）。**「Deploy」** を押す

### ③ コードを貼り付ける
1. デプロイ後の画面で **「Edit code」**（コードを編集）をクリック
2. 表示されているサンプルコードを**全部消す**
3. このリポジトリの **[`cloudflare-worker.js`](cloudflare-worker.js)** の中身を**全部コピーして貼り付け**
4. 右上の **「Deploy」** を押す

### ④ WorkerのURLをコピー
画面に表示される URL（例: `https://market-proxy.あなたの名前.workers.dev`）をコピーします。

### ⑤ GitHubに登録する
1. GitHubのリポジトリ https://github.com/youn24/market-ai-secretary を開く
2. **「Settings」→「Secrets and variables」→「Actions」**
3. **「New repository secret」** をクリック
4. **Name** に `CF_PROXY_URL`、**Secret** に ④でコピーしたURLを貼り付け
5. **「Add secret」** を押す

---

## 完了！

次の朝の自動実行（7:30）から、レポートの業種別ランキングが
**ザラ場中（9:00〜15:30）は約30秒ごとにリアルタイム更新**されるようになります。

- 取引時間外は、その日の最新値を表示します（自動で切り替わります）
- 土日・祝日は動きません（取引が無いため）
- Worker URLを登録しなくても、レポートは今まで通り動きます（朝の値を表示）

## 動作確認
レポートを取引時間中に開き、業種別ランキングの上に
**「🔴 ザラ場・リアルタイム」** と表示されていれば成功です。
